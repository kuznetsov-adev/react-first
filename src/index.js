import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

/**
 * <App /> - компонент который нужно отрисовать
 * 'root' - блок в который мы монтируем компонент
 * */
ReactDOM.render(
    <App/>,
  document.getElementById('root')
);
